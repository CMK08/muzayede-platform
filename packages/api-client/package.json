{
  "name": "@muzayede/api-client",
  "version": "1.0.0",
  "private": true,
  "description": "Type-safe API client for Muzayede Platform",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "@muzayede/shared-types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
