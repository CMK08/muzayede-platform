{
  "name": "@muzayede/shared-utils",
  "version": "1.0.0",
  "private": true,
  "description": "Shared utility functions for Muzayede Platform",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "lint": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
